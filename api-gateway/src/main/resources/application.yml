server:
  port: 8080

spring:
  application:
    name: api-gateway
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:default}

  cloud:
    gateway:
      server:
        webflux:
          globalcors:
            cors-configurations:
              '[/**]':
                allowedOrigins:
                  - "http://localhost:5173"
                  - "http://localhost:3000"
                allowedMethods:
                  - GET
                  - POST
                  - PUT
                  - DELETE
                  - OPTIONS
                allowedHeaders:
                  - "*"
                allowCredentials: true
          routes:
            - id: auth-service
              uri: ${AUTH_SERVICE_URI:http://localhost:8081}
              predicates:
                - Path=/auth/**

            - id: user-service
              uri: ${USER_SERVICE_URI:http://localhost:8082}
              predicates:
                - Path=/api/user/**

            - id: book-service
              uri: ${BOOK_SERVICE_URI:http://localhost:8083}
              predicates:
                - Path=/api/books/**,/api/authors/**,/api/categories/**,/api/publishers/**

            - id: order-service
              uri: ${ORDER_SERVICE_URI:http://localhost:8084}
              predicates:
                - Path=/api/orders/**

            - id: payment-service
              uri: ${PAYMENT_SERVICE_URI:http://localhost:8087}
              predicates:
                - Path=/api/payments/**

            - id: analytics-service
              uri: ${ANALYTICS_SERVICE_URI:http://localhost:8088}
              predicates:
                - Path=/analytics/**

logging:
  level:
    org.springframework.cloud.gateway: INFO
